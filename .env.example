# Copie este arquivo para .env e preencha os valores antes de rodar o docker-compose.
# O arquivo .env nunca deve ser commitado — ele está no .gitignore.

# Senha do PostgreSQL local (desenvolvimento)
DB_PASSWORD=troque_esta_senha_local
