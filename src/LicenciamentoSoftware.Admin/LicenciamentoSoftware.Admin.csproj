<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>LicenciamentoSoftware.Admin</RootNamespace>
    <!-- DAP005: AdminMetricasRepository usa SQL dinâmico com DynamicParameters em runtime
         que é incompatível com Dapper AOT (requer SQL estático analisável em compile-time) -->
    <NoWarn>$(NoWarn);DAP005</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <!-- Acesso direto ao banco via Dapper — reutiliza a camada de persistência -->
    <ProjectReference Include="..\LicenciamentoSoftware.Infrastructure\LicenciamentoSoftware.Infrastructure.csproj" />
    <!-- Handler de reset de 2FA -->
    <ProjectReference Include="..\LicenciamentoSoftware.Application\LicenciamentoSoftware.Application.csproj" />
  </ItemGroup>

</Project>
